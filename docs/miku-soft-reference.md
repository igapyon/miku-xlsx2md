# miku-soft Reference

This repository is maintained as a miku-soft `10 Main Application` project.

The shared miku-soft development references are not copied into this repository.
Use the installed `igapyon-miku-soft-developer` skill as the source of truth for
current cross-project rules and workflow guidance.

- Shared skill repository: <https://github.com/igapyon/igapyon-agent-skills/tree/devel/skills/igapyon-miku-soft-developer>
- Main workflow for this repository: Existing miku-soft Maintenance Workflow
- Owning layer: `10 Main Application`
- Product core source of truth: `src/ts/`
- Downstream Web App surface: `miku-xlsx2md-web`

Historical miku-soft reference snapshots may remain under `docs/` for local
context, but new maintenance decisions should prefer the shared skill reference
above and project-specific documents such as `README.md`, `TODO.md`, and
`docs/TODO.md`.
